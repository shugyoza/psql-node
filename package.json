{
  "name": "psql-node",
  "version": "1.0.0",
  "description": "Plain node.js and postgreSQL project. Minimum node.JS ver.20 for using .env file.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=.env server.js",
    "dev": "nodemon --env-file=.env server.js"
  },
  "keywords": [
    "psql",
    "node.js",
    "postgres",
    "pg",
    "postgreSQL",
    "javascript",
    "JS"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.6",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "pg": "^8.13.0"
  }
}
